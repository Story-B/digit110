start = xml
xml = element xml {source, poem}
source = element source {title, author, date}
title = element title  {attribute level {text}, text}
author = element author {text}
date = element date {text} <!-- i lost ? -->